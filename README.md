# vibe

Tiny, fun, useful CLI for quick focus sprints.

## Run

```bash
python3 vibe.py --mood hype --minutes 1 "clean inbox"
python3 vibe.py --mood hype --minutes 10 --random-task
```

Moods: `calm`, `focus`, `hype`, `cozy`, `creative`.
